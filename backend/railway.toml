# railway.toml - Config as Code for Railway
[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --log-level debug"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "always"
