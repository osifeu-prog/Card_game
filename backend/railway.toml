# railway.toml
[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[start]
command = "uvicorn main:app --host 0.0.0.0 --port 8000"

[deploy]
restartPolicy = "on-failure"
restartRetries = 10

[service]
name = "card_game_backend"
internalPort = 8000
externalPort = 8080
