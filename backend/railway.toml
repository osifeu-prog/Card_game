# railway.toml - Config as Code for Railway
# מגדיר builder, פקודת start, ו‑healthcheck
[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "always"
