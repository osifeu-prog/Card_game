[build]
# ✅ הגדרת ספריית הבסיס ל-Railway
# זה אומר ל-Railway לחפש את ה-Dockerfile, main.py וכו', בתוך ספריית backend.
baseDirectory = "backend"

[deploy]
# 🚀 פקודת ההפעלה (Start Command)
startCommand = "uvicorn main:app --host 0.0.0.0 --port ${PORT}"

# 🩺 בדיקות בריאות (Healthchecks)
healthcheckPath = "/"
healthcheckTimeout = "10s"
healthcheckInterval = "10s"
healthcheckRetries = 3
