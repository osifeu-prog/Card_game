# ... (שלבים קודמים זהים)

# EXPOSE נועד לתיעוד; הפורט האמיתי נלקח ממשתנה הסביבה PORT.
EXPOSE 8080

# פקודת ההפעלה הראשית: שימוש במשתנה הסביבה $PORT.
# הסרת ברירת המחדל המקומית (:-8080) כיוון שב-Railway זה תמיד מוגדר.
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "${PORT}"]
